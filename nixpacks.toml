[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo", "php82Extensions.pdo_mysql", "php82Extensions.mysqli", "php82Extensions.mbstring", "php82Extensions.json"]

[phases.build]
cmds = []

[start]
cmd = "php -S 0.0.0.0:$PORT -t ."
```
5. Clique em **Commit changes**

6. Repita para o `Procfile` com o conteúdo:
```
web: php -S 0.0.0.0:$PORT -t .